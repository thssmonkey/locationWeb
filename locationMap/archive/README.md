## how to run

```bash
# for *nix
export FLASK_APP=recv_lorawan.py; flask run --host=0.0.0.0 --port=7777

# for windows
$env:FLASK_APP="recv_lorawan.py"; flask run --host=183.172.145.42 --port=8000
```
